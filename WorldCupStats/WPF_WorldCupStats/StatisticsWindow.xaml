<Window x:Class="WPF_WorldCupStats.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Statistics" Height="450" Width="800">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Top Scorers" Grid.Column="0" FontWeight="Bold" Margin="10"/>
		<ListBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding TopScorers}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{Binding Name}" Margin="0,0,10,0"/>
						<TextBlock Text="{Binding Goals}"/>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<TextBlock Text="Yellow Cards" Grid.Column="1" FontWeight="Bold" Margin="10"/>
		<ListBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding YellowCards}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{Binding Name}" Margin="0,0,10,0"/>
						<TextBlock Text="{Binding Cards}"/>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</Window>